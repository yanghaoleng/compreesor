# 文件压缩大救星 · 品牌与动效

## 图标概念

正式图标是一台只有头部的矩形小机器人：纸张像面包一样从头顶槽口送入，正面只有两只半垂的眼睛，没有嘴和笑脸。它用最少元素表达“文件进入机器后变轻”，情绪克制、专注，但不被定义为开心或难过。

视觉采用柔和 3D 塑料质感、蓝灰机身、深色面板和暖白纸张。透明背景让同一轮廓可用于亮色与暗色界面；暗色主题额外使用轻微轮廓投影增强可见度。

## 资产

- `docs/assets/robot-paper-icon-source.png`：生成的透明背景母版。
- `public/brand/robot-paper-light.png`：亮色主题与页面标题图标。
- `public/brand/robot-paper-dark.png`：暗色主题与暗色 favicon 入口。
- `public/brand/robot-paper-512.png`：高分辨率应用图标资产。
- `public/apple-touch-icon.png`：iOS 主屏幕图标。

图标应在 32px、44px、132px 三个尺寸保持纸张、槽口和双眼可辨认。禁止在图标下增加实色方形底板、文字或复杂机械细节。

## 品牌文字动效

标题“文件压缩大救星”使用 `soft-blur-in` 的一次性逐字入场：

- 每字 648ms；
- 相邻字符错开 15ms；
- `cubic-bezier(0.22, 1, 0.36, 1)`；
- 从 `opacity: 0 / blur(6px) / translateY(9px)` 进入静止状态；
- 不循环；启用“减少动态效果”时直接显示静态标题。

机器人图标首次进入采用两段独立时间线：先用 `brand-mark-drop-in` 从视口顶部外落下（720ms），落地后再用 `brand-mark-bounce` 回弹（540ms）。hover 与点击只触发 `brand-mark-bounce`，不会重新播放入场动画，也不会重新挂载图片节点；启用“减少动态效果”时动画关闭并直接显示静态图标。

页面其余品牌动效保持克制，避免与压缩进度、预览和打赏动画争夺注意力。

## 控件微动效

- 语言与主题按钮 hover 上浮 2px，边框、柔光和图标角度同步变化；按下时回落并缩小到 96%。
- 语言菜单项 hover 横移 2px，偏好下拉使用同一套边框与柔光反馈。
- 重新处理按钮弹性出现，队列按钮、三档结果卡与无背景操作图标均有轻量 hover / press 反馈。
- 文件行首次出现时使用短促的弹性入场；缩略图只在行 hover 时轻微放大。
- 所有微动效不改变布局尺寸，并在 `prefers-reduced-motion: reduce` 下退化为静态状态。
